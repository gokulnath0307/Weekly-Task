import React from 'react';
import { 

 } from 'react-native';
 const Apptext = (text,style) => {
     return (
         <Text style={style}>{text}</Text>
     );
 }
 
 export default Apptext;